"use client";
import React from "react";


const Header = () => {
 
  return (
    <div>
      {/* <button onClick={() => dispatch(increment())}>ADD</button> */}
    </div>
  );
};

export default Header;
